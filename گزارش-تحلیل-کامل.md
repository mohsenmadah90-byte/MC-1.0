# گزارش تحلیل جامع افزونه MCity Dashboard V2

**تاریخ بررسی:** 2026-07-25  | **نسخه manifest:** 1.14.3 | **شاخه:** `arena/019f98d1-mc-1-0`

## 1. دامنه و روش بررسی

تمام 100 فایل موجود در checkout بررسی شد؛ بررسی نحوی با `node --check *.js` انجام شد و همه فایل‌های JavaScript از نظر syntax معتبر هستند. مجموع کد JavaScript حدود 21,351 خط (با احتساب خط‌های خالی/کامنت) است. import/exportها، وابستگی‌ها، نقاط شروع، دسترسی‌های Script API، ذخیره‌سازی، حلقه‌های زمان‌بندی، مدیریت خطا و مرزهای امنیتی نیز بررسی شدند.

> نکته مهم: فایل‌ها در checkout فعلی همگی در ریشه هستند، نه در پوشه‌هایی که importها انتظار دارند. بنابراین ادعای «بخش‌بندی درست» در سطح کد قابل مشاهده نیست؛ اگر این flatten شدن فقط اثر نحوه آپلود GitHub است، باید در بسته نهایی با ساختار واقعی اصلاح شود.

## 2. نتیجه اجرایی

پروژه از نظر معماری، یک dashboard بزرگ و ماژولار برای سرور Bedrock است و امکانات قابل توجهی دارد: اقتصاد و انتقال پول، level، ATM، قرارداد، زمین/محافظت، بازار، مالی/ledger/payout، audit، backup، notification، migration/sharding، rate-limit، event bus، recovery، hot reload و چندین UI مدیریتی. طراحی تراکنش، checksum/chunk و journal نشان می‌دهد هدف پروژه، دوام داده و بازیابی خطا بوده است.

اما در وضعیت فعلی **قابل انتشار/اجرای مستقیم نیست**. مانع بحرانی، مسیرهای import و مسیر entry manifest است:

* `manifest.json` به `scripts/main.js` اشاره می‌کند، ولی چنین فایل/پوشه‌ای وجود ندارد.
* `main.js` و تقریباً تمام فایل‌ها به مسیرهای `./core/*`، `./dashboard/*`، `./modules/*` و `./schemas/*` import می‌کنند، در حالی که این پوشه‌ها در checkout وجود ندارند.
* تعداد زیادی import با `../` و `../../` نیز با flat بودن فایل‌ها قطعاً resolve نمی‌شوند.
* `node --check` فقط syntax را تأیید می‌کند و resolve شدن moduleها یا سازگاری Bedrock API را تأیید نمی‌کند.

**اولویت صفر:** قبل از هر بهینه‌سازی اقتصادی یا UI، ساختار بسته و import graph باید اصلاح و با یک smoke test واقعی Script API اجرا شود.

## 3. ساختار و امکانات (فایل به فایل)

### هسته و زیرساخت

* `main.js`: bootstrap، ترتیب initialize/shutdown، heartbeat، مالیات زمین، GC قرارداد و event registration.
* `config.js`: پیکربندی مرکزی، سقف‌ها، intervalها و validation.
* `database.js`: لایه persistence بسیار بزرگ با transaction، revision، checksum، slot/chunk و flush.
* `logger.js`: سطح‌بندی log و گزارش startup.
* `bedrockCompat.js`: لایه سازگاری/feature detection برای APIهای Bedrock.
* `errorBoundary.js`: مرزبندی خطا برای UI/اجرای callback.
* `disposableRegistry.js`، `subscriptionRegistry.js`، `runtimeHandleRegistry.js`: lifecycle و پاک‌سازی subscription/interval/job.
* `eventBus.js`، `rateLimiter.js`: ارتباط داخلی و محدودسازی درخواست.
* `playerRegistry.js`: registry بازیکن online و touch/cleanup.
* `memoryMonitor.js`، `scalabilityService.js`: پایش مصرف/اندازه و pruning/scalability.
* `hotReload.js`، `migrations.js`: markerهای reload و مهاجرت داده.
* `batchTaskSchema.js`، `batchTaskService.js`: کارهای صف‌بندی‌شده و batch.
* `operationJournalSchema.js`، `operationJournalService.js`، `appliedOperationStore.js`: idempotency، journal و جلوگیری از اجرای دوباره عملیات.
* `shardRegistry.js`، `shardUtils.js`، `shardMigrationSchema.js`، `shardMigrationService.js`: زیرساخت shard و migration.
* `sanitizer.js`، `permissions.js`، `formUtils.js`، `ux.js`، `uiTheme.js`، `ringBuffer.js`: امنیت ورودی، دسترسی، فرم، UX، theme و buffer.

### اقتصاد و بازیکن

* `moneySchema.js`، `moneyService.js`، `moneyShard.js`، `moneyUtils.js`، `moneyUI.js`: موجودی، انتقال، سقف پول، shard و UI.
* `levelSchema.js`، `levelService.js`، `levelUI.js`: امتیاز/سطح و UI.
* `economyUI.js`، `economyAdminUI.js`، `playerManagementUI.js`، `profileUI.js`: رابط عمومی/مدیریتی اقتصاد و بازیکن.

### ATM

* `atmSchema.js`، `atmService.js`، `atmInventory.js`، `atmLimits.js`، `atmProtection.js`: چرخه کامل ATM، موجودی، محدودیت، حفاظت و ضدسوءاستفاده.
* `atmUI.js`، `atmAdminUI.js`: UI بازیکن و مدیر.

### قرارداد

* `contractSchema.js`، `contractService.js`، `contractInventory.js`، `contractReputation.js`: ایجاد/قبول/تسویه قرارداد، آیتم و reputation.
* `contractShardSchema.js`، `contractShardService.js`: shard قرارداد.
* `contractUI.js`، `contractAdminUI.js`: UI عمومی و مدیریتی.

### زمین

* `landSchema.js`، `landService.js`، `landProtection.js`: claim، مالکیت، فروش، zone و جلوگیری از placement/break غیرمجاز.
* `landTax.js`، `landMarket.js`: مالیات و منطق بازار زمین.
* `landShardService.js`، `landEntryPassShardSchema.js`: shard/entry pass.
* `landAdminUI.js`: UI مدیریتی زمین.

### بازار، مالی و حسابرسی

* `marketSchema.js`، `marketShardSchema.js`: مدل بازار و shard سفارش‌ها.
* `financeSchema.js`، `financeService.js`، `financeLedgerService.js`: خزانه و ledger.
* `financePayoutService.js`، `payoutUI.js`: تخصیص/رزرو/settlement payout با dedupe.
* `financialJournalSchema.js`، `financialJournalService.js`: journal و recovery انتقال‌ها.
* `financialRecoverySchema.js`، `financialRecoveryService.js`: گزارش و بازبینی خرابی مالی.
* `auditSchema.js`، `auditService.js`، `auditAdminUI.js`: audit trail و UI.

### داشبورد، backup و ابزار

* `dashboardSystem.js`، `dashboardEntry.js`، `dashboardRouter.js`، `dashboardNotifications.js`: routing، entry، UI مرکزی و اعلان.
* `adminDashboard.js`: داشبورد مدیر و اتصال به UIهای مدیریتی.
* `backupSchema.js`، `backupBlobSchema.js`، `backupService.js`، `backupAdminUI.js`: backup/restore و blob.
* `notificationSchema.js`: مدل اعلان.
* `itemCatalog.js`، `itemPickerUI.js`، `itemSettingsSchema.js`، `itemSettingsService.js`، `itemUtils.js`: catalog، انتخاب آیتم و تنظیمات.
* `helpUI.js`، `healthCheckUI.js`، `testingToolsUI.js`: راهنما، سلامت و ابزار تست.
* `manifest.json`: metadata و dependencyهای `@minecraft/server 2.1.0` و `@minecraft/server-ui 2.0.0`.

## 4. نقاط قوت

1. جداسازی نسبتاً خوب schema/service/UI و تفکیک دامنه‌ها.
2. توجه جدی به atomicity، revision، checksum، chunking و safe integer برای پول.
3. وجود idempotency/journal و rollback در عملیات مالی پرریسک.
4. lifecycle گسترده برای جلوگیری از interval/subscription leak.
5. سقف‌های عملیاتی برای money، contract، ATM، notification و market.
6. وجود audit، backup، recovery، health check و migration که برای سرور دائمی ارزشمند است.
7. validation مرکزی config و لایه Bedrock compatibility.
8. syntax کل پروژه سالم است و کامنت‌های phase/hotfix، نیت طراحی را روشن می‌کنند.

## 5. ضعف‌ها و راهکار اصلاحی

### بحرانی

**الف) ساختار بسته/مسیرها شکسته است.** راهکار: یکی از دو مدل را انتخاب کنید: (1) پوشه‌های واقعی `scripts/core`, `scripts/dashboard`, `scripts/modules/...`, `scripts/schemas` بسازید و هر فایل را به محل مورد انتظار import منتقل کنید؛ یا (2) همه importها را به flat path تبدیل کنید. مدل اول بهتر و maintainableتر است. سپس `manifest.modules[0].entry` را با مسیر واقعی هماهنگ کنید (`scripts/main.js`) و فقط فایل‌های مورد نیاز را در همان مسیر قرار دهید. یک script CI برای بررسی همه relative importها اضافه شود.

**ب) نبود تست integration با Script API.** راهکار: در محیط Bedrock آزمایشی، smoke test bootstrap، open UI، player join/leave، transaction پول، restart و restore اجرا شود. `node --check` به‌تنهایی کافی نیست.

### مهم

**ج) ریسک ناسازگاری API 2.x.** استفاده از `system.shutdown` و eventهای abstraction باید در نسخه دقیق dependency تست شود. راهکار: همه APIها پشت `BedrockCompat`، با capability check و fallback باشند و نسخه min_engine/dependency در یک ماتریس CI تست شود.

**د) database و serviceهای بزرگ بیش از حد متراکم‌اند.** `database.js` حدود 1739 خط، `landService.js` حدود 1161، `operationJournalService.js` حدود 816، `atmService.js` حدود 779، `moneyService.js` حدود 674 و `financePayoutService.js` حدود 630 خط‌اند. راهکار: استخراج adapter ذخیره‌سازی، transaction primitives، codec/checksum، lock/recovery و domain policy به فایل‌های کوچک‌تر؛ این کار ریسک regression را کم می‌کند.

**هـ) احتمال هزینه I/O و flush بالا.** dynamic property و chunk/checksum اگر در هر transaction یا loop فراخوانی شوند lag ایجاد می‌کنند. راهکار: batch write، dirty-set، flush زمان‌بندی‌شده، backpressure، سقف تعداد عملیات در هر tick و metrics برای مدت transaction/flush.

**و) حلقه‌های متعدد دوره‌ای.** heartbeat، tax، contract GC، ATM limit، recovery، memory/scalability و migration هم‌زمان فعال‌اند. راهکار: scheduler مرکزی با budget هر tick، jitter، اجرای batch محدود و ادامه از cursor؛ از scan کامل collection در interval جلوگیری شود.

**ز) نبود فهرست رسمی تست، lint و type checking.** راهکار: `package.json`، ESLint، Prettier، TypeScript/JSDoc check، unit test برای schema/codec/transaction و fixture برای Dynamic Properties اضافه شود.

**ح) مدیریت secrets و داده حساس.** backup/audit/finance باید retention، محدودیت دسترسی، masking و versioning داشته باشند. راهکار: عدم log کردن payload/موجودی کامل، ACL مستقل برای restore، backup رمزنگاری‌شده در خارج از world property در صورت امکان و تست restore دوره‌ای.

### متوسط

* نسخه پروژه در header/description (`v1.13.2`) با version manifest (`1.14.3`) همسان نیست؛ یک source of truth برای version بسازید.
* `console.error/warn` در config validation باید با Logger سازگار شود تا prefix/سطح و محیط production کنترل شود.
* UIهای زیاد احتمال duplicate form و کد تکراری دارند؛ component/helper مشترک برای pagination، confirmation، خطای فرم و localization ایجاد شود.
* متن‌ها عمدتاً hard-coded هستند؛ resource language و کلیدهای ترجمه استفاده شود.
* برای عملیات حساس، correlation id، actor، قبل/بعد، latency و نتیجه در audit ثبت شود؛ اما داده مالی حساس mask شود.
* schemaها باید migration version، validation سخت‌گیرانه و repair report داشته باشند؛ JSON خراب نباید صرفاً باعث fail کل startup شود.

## 6. برنامه پیشنهادی اجرا

1. **P0:** بازسازی directory و اصلاح manifest/importها؛ اجرای resolver check.
2. **P0:** اجرای bootstrap واقعی روی Bedrock 1.21.100 با dependencyهای اعلام‌شده.
3. **P1:** اضافه‌کردن test harness برای Database، money transfer، payout، land protection و restart recovery.
4. **P1:** scheduler/budget برای loopها و telemetry برای tick latency، DB flush، queue depth و خطا.
5. **P1:** شکستن فایل‌های بزرگ و تعریف interface برای storage و domain service.
6. **P2:** lint/type checks، version automation، localization و مستندسازی API.
7. **P2:** تست بار با بازیکن/transaction/notification مصنوعی و تست restore backup در world کپی.

## 7. جمع‌بندی نهایی

از دید محصول، پروژه نسبتاً کامل و فراتر از یک UI ساده است و برای یک سرور اقتصادی چندماژوله طراحی شده؛ از دید تحویل، مشکل بسته‌بندی و import resolution آن blocker قطعی است. پس از حل ساختار فایل و تأیید bootstrap در Bedrock، بیشترین بازده به‌ترتیب از تست integration، کنترل I/O و scheduler، سپس refactor فایل‌های عظیم و استانداردسازی CI به دست می‌آید.

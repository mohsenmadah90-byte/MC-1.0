# Vanilla Texture Policy

All catalog icon paths target the built-in Vanilla Bedrock texture atlas. No custom item/block PNGs are bundled. Item paths use `textures/items/...`; block paths use `textures/blocks/...`; known Vanilla naming exceptions are mapped explicitly. Every catalog record is marked `textureSource: "vanilla"`.
